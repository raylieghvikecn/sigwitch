var wilddog = require('wilddog-weapp-all');
var gauge = require('gauge-min');

App({
  onLaunch: function () {
    // var config = {
    //   syncURL: 'https://wd2397396495eluilt.wilddogio.com/'
    // };
    // wilddog.initializeApp(config);
    // this.ref = wilddog.sync().ref('Switch');
    // console.log(gauge);
  }
});